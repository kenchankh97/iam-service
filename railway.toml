[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Disable healthcheck temporarily to see actual error logs
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
numReplicas = 1
